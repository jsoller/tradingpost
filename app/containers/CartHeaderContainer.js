import React from 'react';
import PropTypes from 'prop-types'
import CartHeader from '../components/CartHeader';

const CartHeaderContainer = () => (
  <CartHeader title="PURCHASES">
  </CartHeader>
);

export default CartHeaderContainer